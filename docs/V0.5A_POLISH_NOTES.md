# V0.5A Visual Polish Notes

Second-pass polish goals:

- bring the home Hero closer to the scrapbook reference image;
- add original illustrated left album-shelf and right vinyl-corner scenes;
- keep a real uploaded group cover as the central Hero image;
- keep real album covers as small Polaroid overlays;
- reduce Dashboard-like card weight and increase paper / tape / note details;
- keep all existing CRUD, Wishlist, backup and PWA behavior unchanged.

The temporary `app-v0.5a-polish.js` and `v0.5a-polish.css` layers are intentionally isolated for visual review. They should be consolidated into the V0.5 front-end during the later Codex cleanup pass.
